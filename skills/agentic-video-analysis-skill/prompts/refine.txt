これは短い範囲を高fpsで並べたフレームタイル画像です。
各セルには `F<番号> t=<秒>s` のラベルが付いています。

編集点や境界の精密化が目的です。次を確認してください。

1. 対象出来事の開始秒と終了秒
2. その根拠になるセルラベル
3. 前後に余白が必要かどうか
4. まだ不明な点があれば `needs_more_review` として残す

見えていない内容は補完しないでください。秒数はセルラベルの `t=<秒>s` を基準に書いてください。
